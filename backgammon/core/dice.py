
import random
class Dice:
    """Dados del juego.
    Se encarga de generar las tiradas y devolver los valores obtenidos.
    """
class Dice:
    """Representa dos dados de seis caras."""
    def roll(self):
        return random.randint(1, 6), random.randint(1, 6)
